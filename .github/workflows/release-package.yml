name: Build Theme Package

on:
  push:
    branches: [main, development]
    tags: ['v*']
  pull_request:
    branches: [main, development]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  package:
    name: Package wildtours-base-theme
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer

      - name: Install dependencies
        run: composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

      - name: Lint PHP files
        run: |
          if find . -path ./vendor -prune -o -name '*.php' -print0 | xargs -0 -r php -l | grep -v 'No syntax errors'; then
            echo "::error::PHP syntax errors detected"
            exit 1
          fi

      - name: Validate theme structure
        run: |
          test -f style.css
          test -f index.php
          test -f functions.php
          grep -qi '^Theme Name:' style.css
          if grep -qi '^Template:' style.css; then
            echo "::error::Base theme must not declare a Template: header"
            exit 1
          fi

      - name: Build installable zip
        shell: bash
        run: |
          set -euo pipefail
          SLUG="wildtours-base-theme"
          VERSION="$(grep -iE 'Version:' style.css | head -n1 | sed -E 's/.*Version:[[:space:]]*([0-9][^[:space:];]*).*/\1/')"
          VERSION="${VERSION:-dev}"
          PKG_ROOT="${RUNNER_TEMP}/pkg"
          mkdir -p "${PKG_ROOT}/${SLUG}"
          rsync -a \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='.gitignore' \
            --exclude='README.md' \
            --exclude='styles/README.md' \
            --exclude='screenshot.webp' \
            --exclude='*.zip' \
            --exclude='*.bak' \
            --exclude='*.log' \
            --exclude='node_modules/' \
            ./ "${PKG_ROOT}/${SLUG}/"
          ZIP_PATH="${RUNNER_TEMP}/${SLUG}-${VERSION}.zip"
          ( cd "${PKG_ROOT}" && zip -qr "${ZIP_PATH}" "${SLUG}" )
          echo "ZIP_PATH=${ZIP_PATH}" >> "$GITHUB_ENV"
          echo "PACKAGE_VERSION=${VERSION}" >> "$GITHUB_ENV"

      - name: Verify zip structure
        run: |
          unzip -l "${ZIP_PATH}" | grep -q "${SLUG}/style.css"
          unzip -l "${ZIP_PATH}" | grep -q "${SLUG}/index.php"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: wildtours-base-theme-${{ env.PACKAGE_VERSION }}
          path: ${{ runner.temp }}/pkg
          if-no-files-found: error

      - name: Download artifact as delivered
        uses: actions/download-artifact@v4
        with:
          name: wildtours-base-theme-${{ env.PACKAGE_VERSION }}
          path: ${{ runner.temp }}/verify

      - name: Verify downloaded artifact is installable
        run: |
          set -euo pipefail
          SLUG="wildtours-base-theme"
          VERIFY="${{ runner.temp }}/verify"
          test -d "${VERIFY}/${SLUG}"
          test -f "${VERIFY}/${SLUG}/style.css"
          test -f "${VERIFY}/${SLUG}/index.php"

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF_NAME}" "${ZIP_PATH}" \
            --title "wildtours-base-theme ${GITHUB_REF_NAME}" \
            --generate-notes || true
